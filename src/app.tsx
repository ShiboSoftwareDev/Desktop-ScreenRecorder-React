import { createRoot } from "react-dom/client";

const root = createRoot(document.body);
root.render(
  <>
    <h1 className="text-blue-500">Helo from React!</h1>
    <h2 className="text-green-500">Helo from React!</h2>
  </>
);
